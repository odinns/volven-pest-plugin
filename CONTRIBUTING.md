# Contributing

Keep changes small and debt-focused.

Before opening a PR, run:

```bash
composer test:all
```

Do not add new budget types just because they are easy to count. Add them when they expose future cost that existing tools do not already own.
